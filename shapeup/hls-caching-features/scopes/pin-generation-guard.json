{
  "scope_id": "pin-generation-guard",
  "topology_type": "ICEBERG",
  "business_goal": "An in-use or in-flight cache entry is never evicted out from under it, and removing or evicting an entry cancels its in-flight download so it can never resurrect mid-download — pin refcount + generation guard primitives, generalized into the verified temp/verify/promote write path, consumed by explicit removal (R4/R5, A4, UC-PinAndReleaseAsset + UC-RemoveCacheAsset, breadboard slice V4). Extended in the round-4 device-smoke remap (2026-07-26) to own the jest VFS mock's fidelity for the exact write path this scope already owns: BUG-6 (CacheFileRepository.verifyAndPromote -> FileSystemManager.moveFile -> real react-native-blob-util fs.mv FAILS when the destination already exists, breaking second-session re-ingest of an already-cached asset) is fixed in verifiedWrite.ts/fileSystem.ts (already this scope's substrate), and its companion regression-test enabler — making src/__mock__/react-native-blob-util.js's mv() reject on an existing destination instead of silently overwriting, the mock-fidelity gap that let 244 tests pass over a real bug — is this scope's to make, since only this scope's code changes depend on the corrected mock behavior to be provable in jest.",
  "tasks": ["TASK-005", "TASK-006", "TASK-010", "TASK-017"],
  "allowed_file_substrate": [
    "src/Libs/pinGenerationGuard.*",
    "src/Libs/verifiedWrite.*",
    "src/Libs/fileSystem.ts",
    "src/Provider/PreCacheProvider.ts",
    "src/ProxyCacheManager.ts",
    "src/__mock__/react-native-blob-util.js",
    "src/__tests__/pin-cancel*.test.*"
  ],
  "shared_substrate": [
    "src/ProxyCacheManager.ts",
    "src/Provider/PreCacheProvider.ts",
    "src/Libs/fileSystem.ts"
  ],
  "affordance_manifest": [],
  "e2e_verification_fixtures": [
    "yarn test --testPathPattern 'pin-cancel'",
    "yarn test",
    "yarn typecheck"
  ],
  "manual_checks": [
    "TBD(device QA): react-native-blob-util StatefulPromise.cancel() abort fidelity on real iOS/Android devices is not jest-verifiable (pitch's own stated residual unknown) — deferred to the post-PASS QA edge hunt, not a t0 fixture",
    "CAUTION(shared fixture, full suite required): src/__mock__/react-native-blob-util.js is imported by test files across every other scope (signature-rotation, pin-cancel-*, index.test.tsx, issue-5-incomplete-file, full-lifecycle, prefetch-window, registry-eviction, serve-guard, verified-cache-writes, hls-ingest per the 2026-07-25 discovery scan) — none of those scopes have write access to this file, but changing mv() to fail-on-existing-destination changes its OBSERVABLE behavior for all of them; this scope's e2e_verification_fixtures runs the FULL `yarn test` (not just pin-cancel*) specifically to catch any other suite that depended on the old silent-overwrite semantics before this fix ships."
  ],
  "hill_phase": "UPHILL_UNKNOWN"
}
